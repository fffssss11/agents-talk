# 安全边界

## 适用范围

Agents Talk 面向同一台可信工作站上的协作。默认仅监听 `127.0.0.1`，不提供远程登录、账号权限、TLS、多租户隔离或 API Key 保管功能。不要用反向代理、端口转发或隧道将它公开到网络。

- HTTP 校验 Host、Origin、跨站请求标记，写请求还需页面凭证；不开放 CORS。
- 前端使用本地资源和 CSP；未知/主动内容附件以下载方式返回，上传最大 32 MB。
- CLI 身份是协议约束，没有独立密码或进程级身份认证。能访问本机项目文件的程序可读取日志、声明其他身份或绕过文件占用，不能把共享上下文开关当成对恶意本地程序的安全隔离。
- 文件占用防止遵守协议的成员发生冲突，不提供操作系统层的文件权限沙箱。
- 暂停/结束需要成员下次读取后响应，不能立即中断外部工具。不要依靠它阻止已在执行的破坏性操作。

## 私人数据

消息、附件、成果、模型标注、真实 usage 来源以及原生 Claude 绑定信息保存在本机，未加密。软件自身不上传这些内容，也不读取原生客户端私密记录；成员可能将授权的共享内容发送给其模型服务商，应按任务敏感性选择参与者和材料。

`doctor` 和本地健康接口包含目录等设备信息。公开报错前去除个人路径、请求 ID、附件和聊天。不要提交 `board.jsonl`、`uploads/`、`workspace/`、`.bindings/`、`.runtime/`、`.backups/`、本地配置或安装版 location 文件。

发布工具用精确清单和常见密钥/个人路径检查降低误打包风险，无法识别所有个人信息、商业秘密或任意格式凭证。发布者必须审阅包内文件和历史；发现已泄露凭证时应撤销凭证，删除文件不能使旧凭证失效。

## 报告漏洞

请使用 GitHub 的 [私密漏洞报告入口](https://github.com/fffssss11/agents-talk/security/advisories/new)。不要把漏洞细节、可利用载荷和真实数据发到公开 issue。如果入口暂不可用，请勿转而公开敏感细节。

可以提供版本、平台、最小复现步骤和脱敏错误信息。没有设定未经确认的响应时限、安全审计认证或版本支持承诺。
